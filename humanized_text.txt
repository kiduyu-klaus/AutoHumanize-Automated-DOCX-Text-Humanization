Summary of Changes
I just changed your code to regular selenium with chromedriver instead of undetected chromedriver. These are the main modifications:1. finaltexttohuman. py

Replaced undetected_chromedriver with uc and used selenium. webdriver

Updated get_huminizer_chrome_driver() function to use:

Webdriver. chrome() instead of uc. chrome()Options() selenium. webdriver. chrome. optionsInclude CDP commands to bypass automation detection. Incorporated additional stealth measures, such as deactivating automation flags.



2. requirements. 2. les besoins. - Faire simple. - Être clair. - Savoir ce qu’il faut. - Donne ce qui est demandé. - Va droit au but. - Pas trop long. - Donne juste ce qui est utile. Txt

Removed undetected chromedriverSelenium==4.16.0 (standard Selenium). Included webdriver-manager==4.0.1 (optional, for automated driver management)


3. Dockerfile


Added ChromeDriver installation section
Downloads and installs chrome driver version that matches chrome. Places chrome driver in /usr/local/bin/ for system wide access.


Additional Notes:
To use webdriver-manager (automatic driver management), you can modify the driver initialization to: